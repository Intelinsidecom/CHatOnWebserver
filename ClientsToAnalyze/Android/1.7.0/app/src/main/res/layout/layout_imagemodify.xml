<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:gravity="center_vertical"
            android:id="@+id/crop_menu"
            android:background="@drawable/bottom_button_bg"
            android:padding="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true">
            <Button
                android:id="@+id/discard"
                android:text="@string/ams_crop_discard_text"
                style="@style/AddBuddyButtonStyle"/>
            <Button
                android:id="@+id/rotate"
                android:layout_marginLeft="4dp"
                android:text="@string/ams_crop_rotate_image"
                style="@style/AddBuddyButtonStyle"/>
            <Button
                android:id="@+id/save"
                android:layout_marginLeft="4dp"
                android:text="@string/image_view_apply"
                style="@style/AddBuddyButtonStyle"/>
        </LinearLayout>
        <view
            android:id="@+id/image"
            android:background="#55000000"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_x="0dp"
            android:layout_y="0dp"
            android:layout_above="@+id/crop_menu"
            class="com.sec.vip.cropimage.ImageModifyView"/>
    </RelativeLayout>
</FrameLayout>
