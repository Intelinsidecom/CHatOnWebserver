<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatON_WoA.Views"
    xmlns:infra="using:ChatOn.Infrastructure"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:customControl="using:ChatOn.Controls"
    x:Class="ChatOn.Controls.AddBirthdayDialog"
    mc:Ignorable="d" HorizontalAlignment="Left" VerticalAlignment="Top">

    <UserControl.Resources>
    </UserControl.Resources>

    <Border x:Name="LayoutRoot" Background="White" HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="2" BorderBrush="#FF212121">
        <Grid Background="White" VerticalAlignment="Top" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_HEADER_BIRTHDAY'}" VerticalAlignment="Top" FontSize="26.667" FontFamily="Segoe UI Light" FontWeight="SemiLight" Foreground="#FF212121" Margin="20,0,0,0"/>
            <customControl:DateChooser x:Name="DateChooserControl" HorizontalAlignment="Left" Margin="22,10,0,0" Grid.Row="1"/>
            <CheckBox x:Name="ToggleShowYear" IsEnabled="{Binding IsOn, ElementName=ToggleShowBirthday}" Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BODY_SHOW_YEAR_ABB'}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="19,29,0,0" Grid.Row="2"/>
            <StackPanel Grid.Row="3" VerticalAlignment="Top" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,20,20">
                <Button x:ConnectionId='1' Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BUTTON_OK'}"                        HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Button x:ConnectionId='2' Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BUTTON_CANCEL'}"                        HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

