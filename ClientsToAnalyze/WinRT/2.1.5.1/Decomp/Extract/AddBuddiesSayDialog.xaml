<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ChatON_WoA.Views"
             xmlns:infra="using:ChatOn.Infrastructure"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="ChatOn.Controls.AddBuddiesSayDialog"
             mc:Ignorable="d"
             HorizontalAlignment="Left"
             VerticalAlignment="Top">
    <UserControl.Resources>

    </UserControl.Resources>

    <Border x:Name="LayoutRoot"
            Background="White"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            BorderThickness="2"
            BorderBrush="#FF212121">
        <Grid Background="White"
              VerticalAlignment="Top"
              HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Margin="0, 10, 0, 0">
                <TextBlock HorizontalAlignment="Left"
                           TextWrapping="Wrap"
                           Text="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_SK_LITE_ADD'}"
                           VerticalAlignment="Top"
                           FontSize="26.667"
                           FontFamily="Segoe UI Light"
                           Foreground="#FF212121"
                           Margin="20,0,0,0" />
                <TextBlock HorizontalAlignment="Right"
                           TextWrapping="Wrap"
                           x:Name="txtStatusCharacter"
                           VerticalAlignment="Top"
                           FontSize="26.667"
                           FontFamily="Segoe UI Light"
                           FontWeight="SemiLight"
                           Foreground="#FF212121"
                           Margin="0,0,20,0" />
            </Grid>
            <TextBox x:ConnectionId='1' x:Name="InputBox"
                                             
                     AcceptsReturn="True"
                     Grid.Row="1"
                     TextWrapping="Wrap"
                     VerticalAlignment="Top"
                     Margin="20,20,20,10"
                     Style="{StaticResource LightThemeTextBoxStyle}"
                                                                   
                     Width="280"
                     Height="52"
                     MaxLength="140" />
            <StackPanel Grid.Row="2"
                        VerticalAlignment="Top"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,0,20,20">
                <Button x:ConnectionId='2' Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BUTTON_ADD'}"
                                                    
                        Style="{StaticResource WinsetButton}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top" />
                <Button x:ConnectionId='3' Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BUTTON_CANCEL'}"
                                                    
                        Style="{StaticResource WinsetButton}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

