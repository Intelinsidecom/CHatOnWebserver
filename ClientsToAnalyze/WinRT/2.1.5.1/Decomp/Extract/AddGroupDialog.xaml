<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ChatON_WoA.Views"
             xmlns:infra="using:ChatOn.Infrastructure"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="ChatOn.Controls.AddGroupDialog"
             mc:Ignorable="d"
             HorizontalAlignment="Left"
             VerticalAlignment="Top">
    <UserControl.Resources>

    </UserControl.Resources>

    <Border x:Name="LayoutRoot"
            Background="White"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            BorderThickness="2"
            BorderBrush="#FF212121">
        <Grid Background="White"
              VerticalAlignment="Top"
              HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText"
                       HorizontalAlignment="Left"
                       TextWrapping="Wrap"
                       Text="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_HEADER_ADD_GROUP_ABB'}"
                       VerticalAlignment="Top"
                       FontSize="26.667"
                       FontFamily="Segoe UI Light"
                       FontWeight="SemiLight"
                       Foreground="#FF212121"
                       Margin="20,0,0,0" />
            <TextBox x:ConnectionId='1' x:Name="InputBox"
                                             
                                                         
                     Grid.Row="1"
                     TextWrapping="Wrap"
                     VerticalAlignment="Top"
                     Margin="20,10"
                     Style="{StaticResource LightThemeTextBoxStyle}"
                     HorizontalAlignment="Left"
                     Width="280"
                     MaxLength="30" />
            <StackPanel Grid.Row="2"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,0,20,20">
                <Button x:ConnectionId='2' x:Name="OKButton"
                        Style="{StaticResource WinsetButton}"
                        Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BUTTON_OK'}"
                                                
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top" />
                <Button x:ConnectionId='3' x:Name="CancelButton"
                        Content="{Binding Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BUTTON_CANCEL'}"
                                                    
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

