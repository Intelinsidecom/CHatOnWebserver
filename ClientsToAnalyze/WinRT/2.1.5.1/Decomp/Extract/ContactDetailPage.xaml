<Page
    x:Class="ChatON_WoA.Views.ContactDetailPage"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatON_WoA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ContactDetailTemplate">
            <Grid HorizontalAlignment="Left" Height="74">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="42"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Type}" HorizontalAlignment="Left" VerticalAlignment="Center"  FontFamily="Segoe UI" FontSize="20" Foreground="Black"/>
                <TextBlock Margin="0,0,0,10" Grid.Row="1" Text="{Binding Value}" HorizontalAlignment="Left" VerticalAlignment="Center"  FontFamily="Segoe UI" FontSize="20" Foreground="Black" Opacity="0.5"/>
            </Grid>
        </DataTemplate>        
    </Page.Resources>
    
    <Grid Name="LayoutRoot" Background="#FFF4F4F4">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="140">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='1' x:Name="backButton" IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"                         />
            <TextBlock x:Name="ContactNameText" Grid.Column="1" Style="{StaticResource PageHeaderTextStyle}" Foreground="Black"/>
        </Grid>
        <Grid VerticalAlignment="Top" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>                
            </Grid.ColumnDefinitions>
            <ListView IsHitTestVisible="false" Margin="0" Grid.Column="1" x:Name="ContactInfoGridView" ItemsSource="{Binding ContactInfos}" ItemContainerStyle="{StaticResource FullWidthListItemStyle}" ItemTemplate="{StaticResource ContactDetailTemplate}" HorizontalAlignment="Left" VerticalAlignment="Top">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </Grid>
    </Grid>
</Page>

