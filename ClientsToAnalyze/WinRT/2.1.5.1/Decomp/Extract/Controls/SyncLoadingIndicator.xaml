<UserControl
    x:Class="ChatON_WoA.Controls.SyncLoadingIndicator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatON_WoA.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!-- Made By MSCI b.riantono 20121212 --> 
    <Grid Name="LoadingIndicator" Width="2000" Height="384" HorizontalAlignment="Center"  VerticalAlignment="Center" Background="#FF212121">
        <StackPanel Width="680" HorizontalAlignment="Center">

            <TextBlock x:Name="SynchronizeText" Text="{Binding Path=Self, Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BODY_SYNCHRONISE'}" Foreground="White" Style="{StaticResource TabletTextMediumStyle}" Margin="0,20,0,0"/>
            <StackPanel Orientation="Horizontal">
                <ProgressRing Foreground="White" IsActive="True" Height="30" Width="30"/>
                <TextBlock Text="{Binding Path=Self,Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_CHATON_BODY_PLEASE_WAIT_ING'}" 
                           Foreground="White"
                           VerticalAlignment="Center" x:Name="PleaseWaitText"/>
            </StackPanel>
            <TextBlock x:Name="txtIntroText" Text="{Binding Path=Self,Converter={StaticResource ResourceStringConverter}, ConverterParameter='IDS_S_CHATON_SYNC_INTRO'}" 
                       HorizontalAlignment="Left"
                       TextWrapping="Wrap"
                       Foreground="Aquamarine"
                       Style="{StaticResource TabletTextSyncStyle}"
                       Width="500"/>
            
        </StackPanel>
    </Grid>
</UserControl>

