<Page x:ConnectionId='1'
    x:Name="pageRoot"
    x:Class="ChatON_WoA.Views.InputNamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatON_WoA.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:customControl="using:ChatOn.Controls"
                                 
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <AppBar x:Name="appBar" Background="#FFFA8719" IsOpen="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50*"/>
                    <ColumnDefinition Width="50*"/>
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="RightCommands" Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right"  Margin="0,14,0,13">
                    <customControl:ImageButton x:ConnectionId='2' x:Name="NextButton" NormalImage="/Images/Icons/appbtn_next.png" PressedImage="/Images/Icons/appbtn_next_press.png" 
                                               Foreground="White" Text="Next" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,35,0"                           />
                </StackPanel>          
            </Grid>
        </AppBar>
    </Page.BottomAppBar>
    <Grid x:Name="LayoutRoot" Background="#FFF4F4F4">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='4' x:Name="backButton" IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"                           />
            <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Registration" Style="{StaticResource PageHeaderTextStyle}" Foreground="Black"/>
            <Button x:ConnectionId='5' Content="Next" x:Name="NextBtn"                            Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom" MinWidth="100" Grid.Column="1" Margin="0,0,100,35"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="SubText2" HorizontalAlignment="Left" Margin="120,0,0,0" TextWrapping="Wrap" Text="Enter name" VerticalAlignment="Top" FontFamily="Segoe UI Light" FontSize="14.667" Foreground="Black"/>
            <TextBox x:ConnectionId='3' x:Name="InputName"
                                                        
                                               HorizontalAlignment="Left" Height="0" Margin="120,7,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="266" Grid.Row="1" IsSpellCheckEnabled="True" IsTextPredictionEnabled="True"                            MaxLength="30"/>
            <TextBlock x:Name="SubText1" HorizontalAlignment="Left" Margin="120,23,0,0" TextWrapping="Wrap" Text="Enter your real name so your friends can recognize you easily" VerticalAlignment="Top" Grid.Row="2" FontFamily="Segoe UI Light" FontSize="14.667" Foreground="#FF26A0DA"/>
        </Grid>
    </Grid>
</Page>

