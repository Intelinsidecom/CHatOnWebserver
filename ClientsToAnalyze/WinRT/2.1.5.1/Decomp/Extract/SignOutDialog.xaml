<UserControl
    x:Class="ChatON_WoA.SignOutDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatON_WoA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Margin="0,0,0,0" Width="280" Height="153" HorizontalAlignment="Left" VerticalAlignment="Top" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="31"/>
            <RowDefinition Height="74"/>            
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="2" Grid.RowSpan="3"/>
        <TextBlock Text="{Binding ConverterParameter=IDS_CHATON_POP_LOGOUT, Converter={StaticResource ResourceStringConverter}}" HorizontalAlignment="Left" TextWrapping="Wrap" FontFamily="Segoe UI" FontSize="20" VerticalAlignment="Top" Margin="22,13,0,0" Foreground="Black"/>
        <TextBlock Grid.Row="1" Text="{Binding ConverterParameter=IDS_CHATON_POP_LOG_OUT_Q, Converter={StaticResource ResourceStringConverter}}" HorizontalAlignment="Left" TextWrapping="Wrap" FontFamily="Segoe UI" FontSize="14.667" VerticalAlignment="Top" Margin="22,11,0,0" Foreground="Black"/>
        <Button x:ConnectionId='1' Grid.Row="2"                        Content="{Binding ConverterParameter=IDS_CHATON_BUTTON_OK, Converter={StaticResource ResourceStringConverter}}" Margin="40,20,0,0"  VerticalAlignment="Top" MinWidth="90"/>
        <Button x:ConnectionId='2' Grid.Row="2" Width="90"                            Content="{Binding ConverterParameter=IDS_CHATON_BUTTON_CANCEL, Converter={StaticResource ResourceStringConverter}}" Margin="0,20,40,0"  VerticalAlignment="Top" MinWidth="90" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>

