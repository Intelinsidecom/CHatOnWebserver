<UserControl x:Class="ChatOn.Controls.LightContextMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480"
    toolkit:TiltEffect.IsTiltEnabled="True">
    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplate">
            <Grid Margin="0,0,0,16" Tap="ItemTap">
                <TextBlock Tag="tilt" Margin="24,0,0,0" TextWrapping="Wrap" Text="{Binding Text}" FontSize="26" FontFamily="Segoe WP SemiLight">
					    <TextBlock.Foreground>
						    <SolidColorBrush Color="{StaticResource PhoneBackgroundColor}"/>
					    </TextBlock.Foreground>
                </TextBlock>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid x:Name="DisableGrid" Margin="0" Tap="DisableGridTap">
            <Grid.Background>
                <SolidColorBrush Color="{StaticResource PhoneBackgroundColor}"/>
            </Grid.Background>
        </Grid>

        <Grid x:Name="MenuGrid" Margin="0,0,0,200" VerticalAlignment="Bottom" Width ="480" Height="4" Background="{StaticResource PhoneForegroundBrush}" RenderTransformOrigin="0.5,0">
            <Border BorderBrush="{StaticResource PhoneBackgroundBrush}" BorderThickness="0,1" Margin="0"/>
            <ItemsControl Margin="0,15" BorderThickness="1" 
                          ItemTemplate="{StaticResource DataTemplate}" ItemsSource="{Binding MenuItems}"/>
            <Grid.RenderTransform>
                <ScaleTransform/>
            </Grid.RenderTransform>
        </Grid>

    </Grid>
</UserControl>
