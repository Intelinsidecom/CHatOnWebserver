<phone:PhoneApplicationPage 
    x:Class="Samsung.NowHub.AllowLocationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">
    
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="24,17,0,28">
            <TextBlock x:Name="ApplicationTitle" Text="{Binding MyResContainer.IDS_CHATON_BODY_CHATON, Source={StaticResource LocalizedStrings}, Converter={StaticResource UpperCaseConverter}}" Margin="0,0,12,0" FontFamily="Segoe WP Semibold" FontSize="23"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0">
            <StackPanel HorizontalAlignment="Left" Margin="24,20,0,95" Width="432">
                <TextBlock x:Name="txtTitle" TextWrapping="Wrap" FontSize="35" Margin="0,0,0,0" Foreground="{StaticResource PhoneForegroundBrush}"/>
                <TextBlock x:Name="txtMsg" TextWrapping="Wrap" FontSize="23" Margin="0,20,0,0" Foreground="{StaticResource PhoneForegroundBrush}"/>
                <HyperlinkButton x:Name="linkPrivatePolicy" Content="{Binding MyResContainer.WP7_CHATON_PRIVACYSTATEMENT, Source={StaticResource LocalizedStrings}}" FontSize="23" HorizontalAlignment="Right" Margin="0,30,20,0" Foreground="{StaticResource PhoneForegroundBrush}" TargetName="_blank"/>
            </StackPanel>
            <Button x:Name="btOK" Content="{Binding MyResContainer.IDS_CHATON_BUTTON_OK, Source={StaticResource LocalizedStrings}}" Height="71" Margin="8,0,236,8" VerticalAlignment="Bottom" Click="btOK_Click" />
            <Button x:Name="btCancel" Content="{Binding MyResContainer.IDS_CHATON_SK_CANCEL, Source={StaticResource LocalizedStrings}}" Height="71" Margin="0,0,0,8" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="236" Click="btCancel_Click" />
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>
