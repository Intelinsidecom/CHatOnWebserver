<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.AttachCalendarDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">    

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="24,17,0,6">
            <TextBlock x:Name="ApplicationTitle" Text="{Binding MyResContainer.IDS_CHATON_BUTTON_CALENDAR, Source={StaticResource LocalizedStrings},Converter={StaticResource UpperCaseConverter}}" Style="{StaticResource PhoneTextNormalStyle}" FontSize="23" FontFamily="Segoe WP Semibold" Margin="0,0,12,0"/>
            <TextBlock x:Name="PageTitle" Text="{Binding MyResContainer.IDS_CHATON_BODY_DETAILS, Source={StaticResource LocalizedStrings}}" Margin="0,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <TextBlock x:Name="textBlockSubject" Grid.Row="1" Text="{Binding Path=Subject, Mode=OneWay}" FontSize="{StaticResource PhoneFontSizeExtraLarge}" TextWrapping="Wrap" Margin="24,40,24,0" VerticalAlignment="Top" />

        <!--ContentPanel - place additional content here-->
        <ScrollViewer x:Name="ContentPanel" Grid.Row="2" Margin="24,0">
            <StackPanel>
                <TextBlock x:Name="textBlockStartTime" Text="{Binding Path=StartTime, Mode=OneWay}" Foreground="{StaticResource PhoneAccentBrush}" Margin="0,12,0,0"/>
                <TextBlock x:Name="textBlockEndTime" Text="{Binding Path=EndTime, Mode=OneWay}" Foreground="{StaticResource PhoneAccentBrush}" Margin="0"/>
                <TextBlock x:Name="textBlockLocation" Text="{Binding Path=Location, Mode=OneWay}" Foreground="{StaticResource PhoneAccentBrush}" Margin="0,12,0,0"/>
                <TextBlock x:Name="textBlockDetail" Text="{Binding Path=Detail, Mode=OneWay}" Margin="0" TextWrapping="Wrap"/>
            </StackPanel>
            
        </ScrollViewer>
    </Grid>
 
    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton x:Name="SendBtn" IconUri="/icons/appbtn_send.png" Text="Send" Click="SendBarIcon_Click"/>            
            <shell:ApplicationBarIconButton IconUri="/icons/appbtn_cancel.png" Text="Cancel" Click="CancelBarIcon_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
