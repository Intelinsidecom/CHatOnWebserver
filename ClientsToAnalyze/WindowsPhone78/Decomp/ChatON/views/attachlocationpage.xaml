<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.AttachLocationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:Customcontrols="clr-namespace:ChatOn.Controls"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    toolkit:TiltEffect.IsTiltEnabled="True"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0,155,0,0"/>
        <Grid Height="155" Margin="0,0,0,0" Grid.Row="1" VerticalAlignment="Top">
            <TextBlock Margin="24,0,0,108" TextWrapping="Wrap" VerticalAlignment="Bottom" Text="{Binding MyResContainer.IDS_CHATON_BUTTON_LOCATION, Source={StaticResource LocalizedStrings},Converter={StaticResource UpperCaseConverter}}" FontSize="23" FontFamily="Segoe WP Semibold"/>
            <Customcontrols:ImageButton Margin="24,82,0,25" ClickMode="Release" HorizontalAlignment="Left" Click="ImageButton_Click"
                                        NormalImage="/icons/appbtn_search.png" PressedImage="/Images/appbtn_search_press.png"
                                        LightNormalImage="/Images/LightTheme/appbtn_search.png" LightPressedImage="/Images/LightTheme/appbtn_search_press.png"/>
        	<TextBox x:Name="SearchInput" Height="70" Margin="64,0,19,15" TextWrapping="Wrap" VerticalAlignment="Bottom" d:LayoutOverrides="VerticalAlignment"/>
        </Grid>
    </Grid>
 
    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/icons/appbtn_location.png" Text="my location" Click="myLocationButton_Click"/>
            <shell:ApplicationBarIconButton IconUri="/icons/appbtn_send.png" Text="send" Click="sendButton_Click"/>
            <shell:ApplicationBarIconButton IconUri="/icons/appbtn_cancel.png" Text="cancel" Click="cancelButton_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
