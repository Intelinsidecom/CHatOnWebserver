<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.BlockListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">
	<phone:PhoneApplicationPage.Resources>
		<DataTemplate x:Key="BlockListItemTemplate">
			<Grid Width="436" Margin="0,0,0,12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="75"/>
					<ColumnDefinition/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>
                <Image Width="75" Height="75" Visibility="{StaticResource PhoneDarkThemeVisibility}" HorizontalAlignment="Left" VerticalAlignment="Center" Source="/ChatOn;component/Images/buddies_default.png" Stretch="Fill"/>
                <Image Width="75" Height="75" Visibility="{StaticResource PhoneLightThemeVisibility}" HorizontalAlignment="Left" VerticalAlignment="Center" Source="/Images/LightTheme/buddies_default image.png" Stretch="Fill"/>
                <Image Width="75" Height="75" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding ProfileImage}" Stretch="Fill"/>
				<TextBlock TextWrapping="Wrap" Text="{Binding Name}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="32" Grid.Column="1" Margin="18,0,0,0"/>
                <Button Content="{Binding MyResContainer.IDS_CHATON_SK_UNBLOCK, Source={StaticResource LocalizedStrings}}" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Click="UnBlock_Click"/>
			</Grid>
		</DataTemplate>
	</phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0">
            <TextBlock x:Name="ApplicationTitle" Text="{Binding MyResContainer.IDS_CHATON_HEADER_BLOCK_LIST, Source={StaticResource LocalizedStrings},Converter={StaticResource UpperCaseConverter}}" Style="{StaticResource PhoneTextNormalStyle}" Margin="24,24,0,0" FontSize="23" FontFamily="Segoe WP Semibold"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="24,54,24,0">
        	<ListBox ItemTemplate="{StaticResource BlockListItemTemplate}" 
                     ItemsSource="{Binding BlockedList}"/>
            <TextBlock Visibility="{Binding BlockedList.Count, Converter={StaticResource SmallerThanParamToVisibility}, ConverterParameter='1'}" Grid.Row="1" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding MyResContainer.IDS_CHATON_BODY_NO_LISTS, Source={StaticResource LocalizedStrings}}" TextAlignment="Center"/>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>
