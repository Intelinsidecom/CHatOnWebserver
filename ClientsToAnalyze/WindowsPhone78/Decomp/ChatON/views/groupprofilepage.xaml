<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.GroupProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Customcontrols="clr-namespace:ChatOn.Controls"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True"
    toolkit:TiltEffect.IsTiltEnabled="True">

    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="BuddyItemTemplate">
            <Grid Width="442" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="62"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Source="{Binding ProfileImage}" Width="62" Height="62" Stretch="UniformToFill" Margin="0,6,0,0" VerticalAlignment="Top"/>
                <Grid Grid.Column="1" Margin="12,0,0,0" Width="367">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding Name}"
    						TextWrapping="Wrap" FontSize="26" FontFamily="Segoe WP SemiLight" HorizontalAlignment="Left" Margin="0" Foreground="{StaticResource PhoneForegroundBrush}" VerticalAlignment="Bottom"/>
                    <Customcontrols:DynamicHeightBubble Visibility="{Binding Status, Converter={StaticResource StringNullOrEmptyToHide}}"
                                                        Text="{Binding Status}" Grid.Row="1"
                                                        Foreground="Black" FontSize="20"
                                                        TextMargin="0,-10,0,-18" MaxTextWidth="330" Margin="0,0,0,-3"/>
                </Grid>
            </Grid>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid Margin="24,0,24,10" VerticalAlignment="Top" d:LayoutOverrides="VerticalAlignment">
                <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" 
                       Text="{Binding MyResContainer.IDS_CHATON_BODY_CHATON, Source={StaticResource LocalizedStrings},Converter={StaticResource UpperCaseConverter}}"
                       FontSize="23" FontFamily="Segoe WP Semibold" Margin="0,20,0,0" d:LayoutOverrides="GridBox"/>
                <Image Visibility="{StaticResource PhoneDarkThemeVisibility}" HorizontalAlignment="Left" Width="62" Height="62" Margin="0,66,0,0" Source="/ChatOn;component/Images/buddies_default.png"/>
                <Image Visibility="{StaticResource PhoneLightThemeVisibility}" HorizontalAlignment="Left" Width="62" Height="62" Margin="0,66,0,0" Source="/Images/LightTheme/buddies_default image.png"/>
                <TextBlock Tag="tilt" Tap="Rename_Tap" FontSize="26" Text="{Binding Group.Name}" Margin="82,58,0,0" FontFamily="Segoe WP SemiLight" VerticalAlignment="Top" TextTrimming="WordEllipsis"/>
                <TextBlock FontSize="20" Text="{Binding MembersCountText}" Margin="82,0,0,0" FontFamily="Segoe WP" VerticalAlignment="Bottom" TextTrimming="WordEllipsis">
                	<TextBlock.Foreground>
                		<SolidColorBrush Color="{StaticResource PhoneSubtleColor}"/>
                	</TextBlock.Foreground>
                </TextBlock>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button x:Name="ChatButton" Click="Chat_Click" Content="{Binding MyResContainer.IDS_CHATON_HEADER_GROUP_CHAT, Source={StaticResource LocalizedStrings}}" Margin="12,0,0,0" VerticalAlignment="Top"/>
                <Button x:Name="BroadcastButton" Click="Broadcast_Click" Content="{Binding MyResContainer.IDS_CHATON_SK_BROADCAST, Source={StaticResource LocalizedStrings}}" Margin="0,0,12,0" VerticalAlignment="Top" Grid.Column="1"/>
            </Grid>
            <TextBlock Margin="24,15,0,0" Grid.Row="2" TextWrapping="Wrap" Text="{Binding MyResContainer.IDS_CHATON_MBODY_BUDDIES, Source={StaticResource LocalizedStrings}}" VerticalAlignment="Top" FontSize="32" FontFamily="Segoe WP SemiLight"/>
            <ListBox Margin="24,0" Grid.Row="3" ItemsSource="{Binding DisplayItems}" ItemTemplate="{StaticResource BuddyItemTemplate}"/>
        </Grid>
    </Grid>
    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/icons/appbar.edit.rest.png" Text="Edit" Click="Edit_Click"/>
            <shell:ApplicationBarIconButton IconUri="/icons/appbar.delete.rest.png" Text="Delete" Click="Delete_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
