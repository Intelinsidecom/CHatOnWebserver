<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="ApplicationTitle" Text="{Binding Site, Converter={StaticResource UpperCaseConverter}}"  Style="{StaticResource PhoneTextNormalStyle}"/>
        </StackPanel>

        <CheckBox x:Name="checkBoxLogin" Content="" HorizontalAlignment="Left" Margin="12,272,0,0" Grid.Row="1" VerticalAlignment="Top" IsChecked="True"/>
        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <TextBlock x:Name="textBlockEmail" HorizontalAlignment="Left" Margin="12,12,0,0" TextWrapping="Wrap" Text="Email address" VerticalAlignment="Top"/>
            <TextBox x:Name="textBoxUserID" Margin="0,39,0,0" VerticalAlignment="Top" TextInput="textBoxUserID_TextInput" InputScope="EmailSmtpAddress" HorizontalScrollBarVisibility="Hidden"/>
            <TextBlock x:Name="textBlockPassword" HorizontalAlignment="Left" Margin="12,115,0,0" TextWrapping="Wrap" Text="Password" VerticalAlignment="Top"/>
            <PasswordBox x:Name="textBoxPassword" VerticalAlignment="Top" Margin="0,142,0,0"/>
            <Button x:Name="btnLogin" Click="btnLogin_Click"  Content="login" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,214,0,0" Width="216"/>
            <Button x:Name="btnCancel" Click="btnCancel_Click"  Content="cancel" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,214,12,0" Width="216"/>
            <Rectangle x:Name="UploadingBgRect" Fill="Black" Opacity="0.8" Margin="0,-108,0,0" Visibility="Collapsed"/>
            <ProgressBar x:Name="UploadingProgress" Margin="8,324,8,320" IsIndeterminate="True" Visibility="Collapsed"/>
        </Grid>

    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>
