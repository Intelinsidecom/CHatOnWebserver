<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.SearchBuddyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Customcontrols="clr-namespace:ChatOn.Controls"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="480"
    shell:SystemTray.IsVisible="False"
    toolkit:TiltEffect.IsTiltEnabled="True">

    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="SearchedBuddyItemTemplate">
            <Grid Width ="432" Margin="24,0,24,33">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Image Tag="tilt" Source="{Binding Buddy.ProfileImage}" Width="62" Height="62" Stretch="UniformToFill" Tap="Image_Tap"/>
                <Customcontrols:HilightedTextBlock Tap="HilightedTextBlock_Tap" Tag="tilt" MaxLength="30" Text="{Binding Buddy.Name}" Keyword="{Binding Keyword}"
            		FontSize="43" FontFamily="Segoe WP SemiLight" Margin="12,0,0,0" Foreground="{StaticResource PhoneForegroundBrush}" VerticalAlignment="Center" Grid.Column="2"/>
            </Grid>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
    	<Grid.RowDefinitions>
    		<RowDefinition Height="Auto"/>
    		<RowDefinition/>
    	</Grid.RowDefinitions>
		<TextBox Grid.Row="0" x:Name="InputBox" Text="{Binding Keyword, Mode=TwoWay}" TextWrapping="Wrap" Margin="12,0,12,27" TextChanged="InputBox_TextChanged"/>
		<ListBox Grid.Row="1" x:Name="SearchResultList" ItemsSource="{Binding SearchResult}" ItemTemplate="{StaticResource SearchedBuddyItemTemplate}"/>
	</Grid>

</phone:PhoneApplicationPage>
