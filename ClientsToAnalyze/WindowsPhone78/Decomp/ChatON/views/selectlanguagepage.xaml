<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.SelectLanguagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True"
    toolkit:TiltEffect.IsTiltEnabled="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,0">
            <TextBlock x:Name="ApplicationTitle" Text="CHATON" Style="{StaticResource PhoneTextNormalStyle}" FontSize="23" FontFamily="Segoe WP Semibold"/>
            <!-- <TextBlock x:Name="PageTitle" Text="{Binding MyResContainer.IDS_CHATON_HEADER_SELECT_COUNTRY, Source={StaticResource LocalizedStrings},Converter={StaticResource UpperCaseConverter}}" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/> -->
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <StackPanel x:Name="ContentPanel" Grid.Row="1" Margin="24,54,24,0">
            <TextBlock TextWrapping="Wrap" Text="{Binding MyResContainer.IDS_CHATON_HEADER_SELECT_COUNTRY, Source={StaticResource LocalizedStrings}}" Margin="0,0,0,0">
            	<TextBlock.Foreground>
            		<SolidColorBrush Color="{StaticResource PhoneSubtleColor}"/>
            	</TextBlock.Foreground>
            </TextBlock>
            <Grid Tag="tilt" Margin="0,10,0,0" Tap="Select_Tap">
                <Rectangle StrokeThickness="0" Height="48" Stroke="Black" VerticalAlignment="Top">
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{StaticResource PhoneTextBoxColor}"/>
                    </Rectangle.Fill>
                </Rectangle>
                <TextBlock x:Name="SelectedLanguage" Text="{Binding MyResContainer.IDS_CHATON_OPT_NONE, Source={StaticResource LocalizedStrings}}" TextWrapping="Wrap" FontSize="25" VerticalAlignment="Center" Margin="8,0,0,0">
        			<TextBlock.Foreground>
        				<SolidColorBrush Color="{StaticResource PhoneTextBoxForegroundColor}"/>
        			</TextBlock.Foreground>
                </TextBlock>
            </Grid>
        </StackPanel>
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="False">
            <shell:ApplicationBarIconButton IconUri="/Toolkit.Content/ApplicationBar.Check.png" Text="OK" Click="OK_Click" IsEnabled ="False"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
