<phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.ShareDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="ApplicationTitle" VerticalAlignment="Bottom" FontSize="23" FontFamily="Segoe WP Semibold" Style="{StaticResource PhoneTextNormalStyle}"/>
        </StackPanel>
        <ScrollViewer x:Name="ContentPanel" Margin="0,70,0,0">
            <Grid>
            	<Grid.RowDefinitions>
            		<RowDefinition Height="Auto"/>
            		<RowDefinition Height="Auto"/>
            	</Grid.RowDefinitions>
                <Image x:Name="UploadImage" HorizontalAlignment="Center" Stretch="Uniform" MaxHeight="400"/>
                <TextBox x:Name="UploadTextbox" TextWrapping="Wrap" VerticalAlignment="Top" HorizontalScrollBarVisibility="Auto" InputScope="Default" AcceptsReturn="True" Margin="12,0" Grid.Row="1"/>
            </Grid>
        </ScrollViewer>
        <Rectangle x:Name="UploadingBgRect" Fill="Black" Opacity="0.8" Margin="0,-108,0,0" Visibility="Collapsed"/>
        <ProgressBar x:Name="UploadingProgress" Margin="8,324,8,320" IsIndeterminate="True" Visibility="Collapsed"/>
    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Toolkit.Content/ApplicationBar.Check.png" Text="done" Click="doneButton_Click"/>
            <shell:ApplicationBarIconButton IconUri="/Toolkit.Content/ApplicationBar.Cancel.png" Text="cancel" Click="cancelButton_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
