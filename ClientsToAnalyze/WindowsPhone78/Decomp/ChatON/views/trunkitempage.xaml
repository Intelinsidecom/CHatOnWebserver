 <phone:PhoneApplicationPage 
    x:Class="ChatOn.Views.TrunkItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True"
    toolkit:TiltEffect.IsTiltEnabled="True">

    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="TrunkCommentTemplate">
            <Grid Width="432" Hold="Hold_CommentItem" Margin="0,0,0,10">
            	<Grid.RowDefinitions>
            		<RowDefinition Height="Auto"/>
            		<RowDefinition/>
            	</Grid.RowDefinitions>
            	<Grid.ColumnDefinitions>
            		<ColumnDefinition Width="Auto"/>
            		<ColumnDefinition/>
            		<ColumnDefinition Width="Auto"/>
            	</Grid.ColumnDefinitions>
                <Image Grid.Column="0" Grid.RowSpan="2" Visibility="{StaticResource PhoneDarkThemeVisibility}" VerticalAlignment="Top" Source="/ChatOn;component/Images/buddies_default.png" HorizontalAlignment="Left" Width="75"/>
                <Image Grid.Column="0" Grid.RowSpan="2" Visibility="{StaticResource PhoneLightThemeVisibility}" VerticalAlignment="Top" Source="/Images/LightTheme/buddies_default image.png" HorizontalAlignment="Left" Width="75"/>
                <Image Grid.Column="0" Grid.RowSpan="2" VerticalAlignment="Top" Source="{Binding  ThumbImage}" HorizontalAlignment="Left" Width="75" />
                <TextBlock Grid.Column="1" TextWrapping="Wrap" Text="{Binding Name}" VerticalAlignment="Top" FontSize="28" FontFamily="Segoe WP Light" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                <TextBlock Grid.Column="2" HorizontalAlignment="Right" TextWrapping="Wrap" Text="{Binding Regdttm}" FontSize="17" Foreground="#FF7C7777" FontFamily="Segoe WP Semibold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding Content}" FontSize="20" Foreground="{StaticResource PhoneAccentBrush}" FontFamily="Segoe WP" VerticalAlignment="Top" Margin="10,0,0,0" />
            </Grid>
        </DataTemplate>


    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="0,0,0,10" Height="88">
                <TextBlock x:Name="ApplicationTitle" Text="{Binding UploaderName}" Style="{StaticResource PhoneTextNormalStyle}" FontSize="43" FontFamily="Segoe WP SemiLight" VerticalAlignment="Top" Margin="24,0" >
                	<TextBlock.Foreground>
                		<SolidColorBrush Color="{StaticResource PhoneSubtleColor}"/>
                	</TextBlock.Foreground>
                </TextBlock>
                <TextBlock x:Name="PageTitle" Text="{Binding UploadTime}" Style="{StaticResource PhoneTextTitle1Style}" FontSize="23" Foreground="#FF666666" FontFamily="Segoe WP Semibold" Margin="24,0" VerticalAlignment="Top" />
            </StackPanel>

            <ScrollViewer Grid.Row="1" Margin="0,0,0,65" VerticalAlignment="Stretch">
                <Grid x:Name="ContentPanel">
                    <Grid Width="480" Height="300" VerticalAlignment="Top">
                        <Image Name="image2" Source="{Binding ItemImg}" VerticalAlignment="Center" HorizontalAlignment="Center" MouseLeftButtonUp="OpenFile" Stretch="UniformToFill" />
                    </Grid>
                    <Image Height="300" HorizontalAlignment="Center" VerticalAlignment="Top" Width="450" Source="/ChatOn;component/Images/icon_video.png" Margin="0" Visibility="{Binding Video_Visible}" MouseLeftButtonUp="OpenVideoFile" />
                    <Image Height="113" HorizontalAlignment="Left" VerticalAlignment="Top" Width="113" Source="/Images/ams_play.png" Margin="183,93,0,0" Visibility="{Binding Play_Visible}" MouseLeftButtonUp="OpenAMSPlay" />

                    <Button Content="{Binding MyResContainer.IDS_CHATON_BODY_PREVIOUS_COMMENTS, Source={StaticResource LocalizedStrings}}" Height="58" Margin="12,301,0,0" Name="PreviousButton" VerticalAlignment="Top" FontSize="18" Width="350" HorizontalAlignment="Left" Click="PreviousBtnClick" Visibility="{Binding PrevioseBtn_Visible}" BorderThickness="1">
                    	<Button.Foreground>
                    		<SolidColorBrush Color="{StaticResource PhoneAccentColor}"/>
                    	</Button.Foreground>
                    </Button>
                    <TextBlock HorizontalAlignment="Right" Margin="0,313,52,0" Name="textBlock2" Text="{Binding CommentCnt}" VerticalAlignment="Top" TextAlignment="Right" Foreground="{StaticResource PhoneAccentBrush}" />
                    <Image HorizontalAlignment="Right" Margin="0,318,24,0" Name="image3" Stretch="None" VerticalAlignment="Top" Source="/Images/icon_comments.png" />
                    <ListBox Margin="24,360,0,0" ItemTemplate="{StaticResource TrunkCommentTemplate}" ItemsSource="{Binding TrunkComments}" VerticalAlignment="Top" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>

                </Grid>
            </ScrollViewer>

            <Grid x:Name="BtnPanel" Grid.Row="2" Margin="0,0,0,0" >
                <Button Content="{Binding MyResContainer.IDS_CHATON_SK3_ADD_COMMENT, Source={StaticResource LocalizedStrings}}" Margin="12,0" Name="button1" VerticalAlignment="Bottom" FontSize="18" Click="WriteCommentClick" Height="70" />
            </Grid>
        </Grid>

    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/icons/appbtn_share.png" Text="Share" x:Name="menu_trunkitem_share" Click="Menu_turnkcomment_share_Click" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar> 

</phone:PhoneApplicationPage>
